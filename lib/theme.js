import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
		mode: 'dark',
    primary: {
      main: '#242629',
    },
    secondary: {
      main: '#16161a',
    },
  },
});

export default theme;